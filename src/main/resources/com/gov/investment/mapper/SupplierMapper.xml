<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gov.investment.mapper.SupplierMapper">

    <resultMap id="SupplierResultMap" type="com.gov.investment.model.Supplier">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="type" property="type"/>
        <result column="contact_person" property="contactPerson"/>
        <result column="contact_phone" property="contactPhone"/>
        <result column="contact_email" property="contactEmail"/>
        <result column="address" property="address"/>
        <result column="remark" property="remark"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <insert id="insert" parameterType="com.gov.investment.model.Supplier" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO supplier (name, type, contact_person, contact_phone, contact_email, address, remark, status, create_time, update_time)
        VALUES (#{name}, #{type}, #{contactPerson}, #{contactPhone}, #{contactEmail}, #{address}, #{remark}, #{status}, #{createTime}, #{updateTime})
    </insert>

    <update id="update" parameterType="com.gov.investment.model.Supplier">
        UPDATE supplier
        SET name = #{name}, type = #{type}, contact_person = #{contactPerson}, contact_phone = #{contactPhone},
            contact_email = #{contactEmail}, address = #{address}, remark = #{remark}, status = #{status}, update_time = #{updateTime}
        WHERE id = #{id}
    </update>

    <delete id="deleteById" parameterType="java.lang.Long">
        DELETE FROM supplier WHERE id = #{id}
    </delete>

    <select id="selectById" parameterType="java.lang.Long" resultMap="SupplierResultMap">
        SELECT * FROM supplier WHERE id = #{id}
    </select>

    <select id="selectAll" resultMap="SupplierResultMap">
        SELECT * FROM supplier
    </select>

    <select id="selectByCondition" resultMap="SupplierResultMap">
        SELECT * FROM supplier
        WHERE 1=1
        <if test="name != null and name != ''">
            AND name LIKE CONCAT('%', #{name}, '%')
        </if>
        <if test="type != null and type != ''">
            AND type = #{type}
        </if>
        <if test="status != null and status != ''">
            AND status = #{status}
        </if>
    </select>
</mapper>