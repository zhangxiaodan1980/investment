<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gov.investment.mapper.UserInfoMapper">
    <resultMap id="userResultMap" type="com.gov.investment.model.UserInfo">
        <id property="id" column="id"/>
        <result property="username" column="user_name"/>
        <result property="email" column="email"/>
        <result property="password" column="password"/>
        <result property="roleName" column="role_name"/>
        <result property="enabled" column="enabled"/>
    </resultMap>

    <select id="selectUserByUsername" resultMap="userResultMap">
        SELECT id, user_name, email,password,role_name,enabled FROM users WHERE user_name = #{username}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (user_name, password, email, enabled,role_name)
        VALUES (#{username}, #{password}, #{email}, #{enabled},#{roleName})
    </insert>
</mapper>
